@model Chamedoon.Application.Services.Account.Users.ViewModel.UserDetails_VM
@using Chamedoon.Application.Common.Extensions
@{
    Layout = "~/Views/Shared/UserPanelLayout.cshtml";
    ViewData["Title"] = "پنل کاربری " + User.Identity?.Name;
}

@section UserPanelContent {
    <div class="glass-card dashboard-card">
        <h5>خلاصه حساب کاربری</h5>
        <p class="glass-helper">اطلاعات شخصی و وضعیت حساب تو در این بخش نمایش داده می‌شود.</p>
        <div class="profile-details">
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📛</span>
                <div class="detail-body">
                    <p class="detail-title">نام کاربری</p>
                    <p class="detail-value">@Model.UserName</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📧</span>
                <div class="detail-body">
                    <p class="detail-title">ایمیل</p>
                    <p class="detail-value">@Model.Email</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📅</span>
                <div class="detail-body">
                    <p class="detail-title">تاریخ عضویت</p>
                    <p class="detail-value">@Model.Created.ConvertMiladiToShamsi()</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">⚙️</span>
                <div class="detail-body">
                    <p class="detail-title">وضعیت حساب</p>
                    <p class="detail-value">@(Model.IsBlocked == true ? "فعال" : "غیرفعال")</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">🎯</span>
                <div class="detail-body">
                    <p class="detail-title">مهارت‌های ثبت‌شده</p>
                    <p class="detail-value">html، css، js، mysql</p>
                </div>
            </div>
        </div>
    </div>

    <div class="glass-card dashboard-card mt-4">
        <h5>وضعیت اشتراک</h5>
        <p class="glass-helper">مشخصات اشتراک فعلی و تعداد استعلام‌های باقی‌مانده.</p>
        @if (ViewBag.SubscriptionStatus is Chamedoon.Application.Services.Subscription.SubscriptionStatus status)
        {
            <div class="profile-details">
                <div class="detail-row">
                    <span class="detail-icon" aria-hidden="true">📦</span>
                    <div class="detail-body">
                        <p class="detail-title">پلن فعال</p>
                        <p class="detail-value mb-0">@status.PlanTitle</p>
                        <small class="text-muted">@status.RemainingDays روز باقی مانده</small>
                    </div>
                </div>
                <div class="detail-row">
                    <span class="detail-icon" aria-hidden="true">🧭</span>
                    <div class="detail-body">
                        <p class="detail-title">محدودیت استعلام</p>
                        @if (status.EvaluationLimit is null)
                        {
                            <p class="detail-value">نامحدود</p>
                        }
                        else
                        {
                            <p class="detail-value">@status.UsedEvaluations از @status.EvaluationLimit استفاده شده</p>
                        }
                    </div>
                </div>
                <div class="detail-row">
                    <span class="detail-icon" aria-hidden="true">🤖</span>
                    <div class="detail-body">
                        <p class="detail-title">هوش مصنوعی</p>
                        <p class="detail-value">@(status.IncludesAI ? "فعال" : "در دسترس نیست")</p>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="alert alert-warning" role="alert">
                هنوز اشتراکی فعال نکرده‌ای. از طریق <a asp-controller="Subscription" asp-action="Index" class="alert-link">صفحه اشتراک‌ها</a> یکی را انتخاب کن.
            </div>
        }
    </div>
}

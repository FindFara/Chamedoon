@model Chamedoon.Application.Services.Account.Users.ViewModel.UserDetails_VM
@using Chamedoon.Application.Common.Extensions
@{
    Layout = "~/Views/Shared/UserPanelLayout.cshtml";
    ViewData["Title"] = "پنل کاربری " + User.Identity?.Name;
}

@section UserPanelContent {
    <div class="glass-card dashboard-card">
        <h5>خلاصه حساب کاربری</h5>
        <p class="glass-helper">اطلاعات شخصی و وضعیت حساب تو در این بخش نمایش داده می‌شود.</p>
        <div class="profile-details">
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📛</span>
                <div class="detail-body">
                    <p class="detail-title">نام کاربری</p>
                    <p class="detail-value">@Model.UserName</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📧</span>
                <div class="detail-body">
                    <p class="detail-title">ایمیل</p>
                    <p class="detail-value">@Model.Email</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">📅</span>
                <div class="detail-body">
                    <p class="detail-title">تاریخ عضویت</p>
                    <p class="detail-value">@Model.Created.ConvertMiladiToShamsi()</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">⚙️</span>
                <div class="detail-body">
                    <p class="detail-title">وضعیت حساب</p>
                    <p class="detail-value">@(Model.IsBlocked == true ? "فعال" : "غیرفعال")</p>
                </div>
            </div>
            <div class="detail-row">
                <span class="detail-icon" aria-hidden="true">🎯</span>
                <div class="detail-body">
                    <p class="detail-title">مهارت‌های ثبت‌شده</p>
                    <p class="detail-value">html، css، js، mysql</p>
                </div>
            </div>
        </div>
    </div>
}

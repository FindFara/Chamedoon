@model Chamedoon.Application.Services.Account.Login.ViewModel.LoginUserViewModel
@{
    ViewData["Title"] = "ورود به چمدون";
    Layout = "~/Views/Shared/AuthLayout.cshtml";
}

<section class="glass-section auth-section login-centered">
    <div class="container">
        <div class="auth-card-shell">
            <div class="glass-card auth-card auth-card-simple text-center">
                <div class="auth-icon-badge" aria-hidden="true">
                    🔒
                </div>
                <div class="auth-card-header text-center">
                    <h2>ورود یا ساخت حساب در چمدون</h2>
                    <p class="auth-subtitle">با نام کاربری یا ایمیل</p>
                </div>
                <form asp-action="Login" method="post" novalidate data-prevent-multi-submit="true" class="auth-form">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="requestNonce" value="@(ViewData["LoginNonce"] as string)" />
                    <div class="glass-field">
                        <label asp-for="Identifier" class="form-label text-end w-100">ایمیل یا نام کاربری</label>
                        <input asp-for="Identifier" type="text" class="form-control glass-input" placeholder="example@mail.com" />
                        <span asp-validation-for="Identifier" class="text-danger small"></span>
                    </div>
                    <div class="glass-field">
                        <label asp-for="Password" class="form-label text-end w-100">رمز عبور</label>
                        <input asp-for="Password" type="password" class="form-control glass-input" placeholder="••••••••" />
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>
                    <div class="auth-secondary-actions justify-content-between">
                        <label class="glass-checkbox">
                            <input asp-for="RememberMe" type="checkbox" />
                            <span>مرا به خاطر بسپار</span>
                        </label>
                        <a asp-controller="Account" asp-action="OtpLogin" class="auth-otp-link">ورود با رمز یکبار مصرف</a>
                    </div>
                    <div class="text-danger small" asp-validation-summary="All"></div>
                    <button type="submit" class="btn-auth-primary w-100" data-processing-text="در حال پردازش...">ادامه</button>
                </form>
                <div class="auth-divider">یا ورود با</div>
                <a asp-controller="Account" asp-action="login-google" class="btn-google w-100">
                    <span aria-hidden="true">🔐</span>
                    ورود با حساب گوگل
                </a>
                <div class="auth-meta text-center">
                    <span>هنوز عضو نیستی؟ <a asp-controller="Account" asp-action="Register">ثبت‌نام سریع</a></span>
                </div>
            </div>
        </div>
    </div>
</section>

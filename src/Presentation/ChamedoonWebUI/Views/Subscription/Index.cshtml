@model ChamedoonWebUI.Models.SubscriptionPageViewModel
@{
    ViewData["Title"] = "انتخاب اشتراک";
}

@section Styles {
    <link rel="stylesheet" href="~/css/subscription.css" asp-append-version="true" />
}

<section class="subscription-page py-5 py-lg-6">
    <div class="container">
        <div class="subscription-header">
            <div>
                <p class="subscription-badge">ارزیابی هوشمند مهاجرت</p>
                <h1 class="mb-2">اشتراک مناسب خودت را انتخاب کن</h1>
                <p class="text-muted mb-0">با اشتراک فعال، ارزیابی شانس مهاجرت همیشه در دسترس توست. روی موبایل هم همه چیز خوانا و سریع است.</p>
            </div>
            <div class="subscription-status-card">
                <p class="status-title">وضعیت اشتراک فعلی</p>
                @if (Model.CurrentSubscription is null)
                {
                    <p class="status-value text-muted">هنوز اشتراکی فعال نشده است.</p>
                }
                else
                {
                    <p class="status-value">@Model.CurrentSubscription.PlanTitle</p>
                    <p class="status-meta">@Model.CurrentSubscription.RemainingDays روز باقی مانده</p>
                    @if (Model.CurrentSubscription.EvaluationLimit is not null)
                    {
                        <p class="status-meta">استعلام استفاده شده: @Model.CurrentSubscription.UsedEvaluations از @Model.CurrentSubscription.EvaluationLimit</p>
                    }
                    else
                    {
                        <p class="status-meta">استعلام نامحدود</p>
                    }
                }
            </div>
        </div>

        @if (!string.IsNullOrWhiteSpace(Model.AlertMessage))
        {
            <div class="alert alert-primary subscription-alert" role="alert">
                @Model.AlertMessage
            </div>
        }

        <div class="row g-4">
            @foreach (var plan in Model.Plans)
            {
                var isCurrent = Model.CurrentSubscription?.PlanId == plan.Id;
                <div class="col-md-4">
                    <div class="subscription-card @(plan.IncludesAI ? "subscription-card--ai" : string.Empty) @(isCurrent ? "subscription-card--active" : string.Empty)">
                        <div class="card-header">
                            <div>
                                <p class="card-duration">@plan.DurationLabel</p>
                                <h3 class="card-title">@plan.Title</h3>
                            </div>
                            @if (plan.IncludesAI)
                            {
                                <span class="pill">هوش مصنوعی</span>
                            }
                            else if (plan.EvaluationLimit is null)
                            {
                                <span class="pill pill-secondary">نامحدود</span>
                            }
                        </div>
                        <p class="card-price">@plan.Price.ToString("N0") <span>تومان</span></p>
                        <ul class="feature-list">
                            @foreach (var feature in plan.Features)
                            {
                                <li>
                                    <span aria-hidden="true">✔</span>
                                    <span>@feature</span>
                                </li>
                            }
                            @if (plan.EvaluationLimit is not null)
                            {
                                <li>
                                    <span aria-hidden="true">🎯</span>
                                    <span>حداکثر @plan.EvaluationLimit استعلام در ماه</span>
                                </li>
                            }
                            else
                            {
                                <li>
                                    <span aria-hidden="true">♾️</span>
                                    <span>استعلام نامحدود بدون محدودیت</span>
                                </li>
                            }
                        </ul>
                        <form asp-action="Subscribe" method="post" class="mt-3">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="planId" value="@plan.Id" />
                            <button type="submit" class="btn btn-primary w-100" @(isCurrent ? "disabled" : string.Empty)>
                                @(isCurrent ? "فعال شده" : "شروع اشتراک")
                            </button>
                        </form>
                    </div>
                </div>
            }
        </div>
    </div>
</section>

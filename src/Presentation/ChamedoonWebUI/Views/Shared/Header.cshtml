<nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
    <div class="container">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">Chamedoon</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#primaryNavigation" aria-controls="primaryNavigation" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="primaryNavigation">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Blog" asp-action="Index">Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Immigration" asp-action="Index">Immigration Test</a>
                </li>
            </ul>
            <div class="d-flex align-items-center gap-2">
                <form class="d-none d-lg-flex" asp-controller="Blog" asp-action="Index" method="get">
                    <div class="input-group">
                        <input type="text" name="FilterTitle" class="form-control" placeholder="Search blog" />
                        <button class="btn btn-outline-primary" type="submit">Search</button>
                    </div>
                </form>
                @if (User.Identity?.IsAuthenticated ?? false)
                {
                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            @User.Identity!.Name
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow">
                            <li><a class="dropdown-item" asp-controller="UserPanel" asp-action="Index">Dashboard</a></li>
                            @if (User.Identity?.Name == "Admin")
                            {
                                <li><a class="dropdown-item" asp-area="Admins" asp-controller="Dashboard" asp-action="Index">Admin Panel</a></li>
                            }
                            <li><hr class="dropdown-divider" /></li>
                            <li><a class="dropdown-item" asp-controller="Account" asp-action="Logout">Sign out</a></li>
                        </ul>
                    </div>
                }
                else
                {
                    <a class="btn btn-outline-secondary" asp-controller="Account" asp-action="Login">Sign in</a>
                    <a class="btn btn-primary" asp-controller="Account" asp-action="Register">Create account</a>
                }
            </div>
        </div>
    </div>
</nav>
